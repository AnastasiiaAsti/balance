<%- include('../partials/header') %>


    <div class="upper-bg ">
        <div>
            <a href="/asanas">
                <i class="fa-solid fa-arrow-left text-dark m-4"></i>
            </a>
        </div>
        <div class="welcome-title">
            <h5 class="text-center">Child's Pose
            </h5>
        </div>
        <div class="text-center">
            <img class="img-fluid" src="<%= asana.photosURLs %>" width="">
        </div>
        <% if (user) { %>
            <div class="upper-bg-2">
                <!-- <div class="">
                    <a href="/asanas/<%= asana._id%>/edit"
                        class="btn btn-color btn-sm mt-5 rounded-pill float-right">Update Asana</a>
                </div> -->
                <% } %>
                    <div class="heading mb-3">sankskrit name: </div>
                    <span class="btn-peach-fade">
                        <%= asana.originName %>
                    </span>
                    <div class="heading mt-4 mb-4">
                        <h3>Description: </h3>
                    </div>
                    <div class="peach-fade p-2">
                        <p>
                            <%= asana.description %>
                        </p>

                    </div>
                    <% if (user) { %>
                        <!-- <div class="mt-4">
                            <form action="/asanas/<%= asana._id %>?_method=DELETE" method="POST" id="deleteAsana">
                                <button type="submit" form="deleteAsana"
                                    class="btn border-secondary btn-circle">X</button>
                            </form>
                        </div> -->
                        <% } %>
            </div>
    </div>

    </main>



    <%- include('../partials/footer') %>